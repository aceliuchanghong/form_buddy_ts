## 表单填写精灵

浏览器插件,根据上传的个人文档(可选),自动填写网页待填写的表单

### 快速开始

```bash
# 安装依赖
npm install

# 开发模式运行 直接运行 TS 文件，无需编译
npm run dev

# 构建 编译 TS → JS，输出到 dist/ 目录
npm run build

# 生产模式运行 运行编译后的 dist/index.js
npm start
```
